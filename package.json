{
  "name": "elec-excel2json",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron . --inspect-brk=5858",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . xlsx2json --platform=win32 --arch=x64 --icon=favicon.ico --out=./dist --asar --app-version=0.0.1 --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^13.6.6",
    "electron-packager": "^13.1.1"
  },
  "dependencies": {
    "json-format": "^1.0.1",
    "node-xlsx": "^0.15.0"
  }
}
